Guest endpoints:
    $L /colors
    $L /users

Admin endpoints:
    // **List and filter** _extended colors_
    @token GET /colors (admin)?filter?&page?:i
        => {colors: ColorExtended[], totalCount: i}

    @token $CRUD /colors
    @token $CRUD /users

