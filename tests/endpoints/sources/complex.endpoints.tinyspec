# A comment

My tag:
    // Update resources
    @token PATCH /parent/:a/resources/:b (variant)?c&d?&e:boolean {f, g?, h: i[]}
        => 200 {resource: Resource} # Another comment
        => 404 NotFoundError
